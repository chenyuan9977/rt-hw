
"1. Singleton -> Lazy loading + Eager Loading 
   a. what's singleton 
   b. Why singleton 
   c. code implementation

What is Singleton?
Only one instance in JVM.  
Provide a global access point.

Why Singleton?
- Save memory
- Improve performance
- Shared resource

Lazy Loading — 用到时才创建
public class SingletonLazy {
    
    // 1. 私有静态变量，初始为 null（不提前创建）
    private static SingletonLazy instance = null;
    
    // 2. 私有构造器，外部无法 new
    private SingletonLazy() {
        System.out.println("Creating instance...");
    }
    
    // 3. 全局访问点
    public static SingletonLazy getInstance() {
        if (instance == null) {          // 第一次调用才创建
            instance = new SingletonLazy();
        }
        return instance;
    }
}

// 使用
SingletonLazy a = SingletonLazy.getInstance();  // "Creating instance..."
SingletonLazy b = SingletonLazy.getInstance();  // 直接返回，不再创建
System.out.println(a == b);  // true ✅



🔒 Thread-Safe Lazy Loading — 双重检查锁（推荐）
public class SingletonDoubleCheck {

    // volatile 防止指令重排序（JVM 优化可能导致返回未初始化的对象）
    private static volatile SingletonDoubleCheck instance = null;

    private SingletonDoubleCheck() {}

    public static SingletonDoubleCheck getInstance() {
        if (instance == null) {                    // 第一次检查（无锁，快速路径）
            synchronized (SingletonDoubleCheck.class) {  // 加锁
                if (instance == null) {            // 第二次检查（锁内，安全）
                    instance = new SingletonDoubleCheck();
                }
            }
        }
        return instance;
    }
}

为什么要检查两次？
线程A              线程B
───────────────    ───────────────
instance == null   instance == null   ← 都通过第一次检查
获得锁             等待锁...
创建实例
释放锁             获得锁
                   instance != null   ← 第二次检查拦住了！
                   直接返回

⚡ Eager Loading — 类加载时立即创建
public class SingletonEager {

    // JVM 保证类加载时只执行一次，天然线程安全
    private static final SingletonEager instance = new SingletonEager();

    private SingletonEager() {}

    public static SingletonEager getInstance() {
        return instance;
    }
}




2. research on Strategy Design Pattern 
   a. what is 
   b. why
   c. how you implment in your project
a. What is Strategy Pattern?
Strategy is a behavioral design pattern that defines a family of algorithms, encapsulates each one into a separate class, and makes them interchangeable — without changing the client code that uses them.

Core idea: Extract what varies, program to an interface, not an implementation.

Real-world analogy: Navigation App

Your destination stays the same (Context)
But you can choose: Drive / Bike / Walk (Strategy)
Switch anytime — the app still works perfectly

b. Why Strategy Pattern?
❌ Without Strategy — the problem
javapublic class PaymentService {
    public void pay(String type, double amount) {
        if (type.equals("credit")) {
            System.out.println("Paying " + amount + " via Credit Card");
        } else if (type.equals("paypal")) {
            System.out.println("Paying " + amount + " via PayPal");
        } else if (type.equals("crypto")) {
            System.out.println("Paying " + amount + " via Crypto");
        }
        // Adding a new method → modify this class → violates Open/Closed Principle ❌
    }
}
ProblemDescriptionViolates Open/ClosedEvery new method requires modifying existing codeViolates Single ResponsibilityOne class handles all payment logicHard to testCan't unit test each payment type in isolationif-else explosionGets unmanageable as business grows
✅ What Strategy solves

Add new algorithm → add a new class, touch nothing else (Open/Closed Principle)
Algorithms can be swapped at runtime dynamically
Each strategy is independently testable
Eliminates if-else / switch chains




Mock:
1. What is the Singleton design pattern?
Singleton guarantees a class has only one instance across the whole application, with one global access point. The best real example is Spring beans, which are singletons by default. To build it safely I make the constructor private and hold one static instance, and the cleanest thread-safe way in Java is an enum or a static holder class. It fits shared, stateless things like configuration or a connection pool.

2. Where can we set CORS — backend, frontend, or both?
CORS is enforced by the browser but configured on the backend — the frontend can't grant itself permission, the server decides which origins are allowed through response headers. In Spring Boot I set it with the CrossOrigin annotation locally, or globally with a CORS config bean. In microservices I centralize it at the API gateway so I don't repeat it in every service.

3. Can you write a hint in Hibernate?
Yes. JPA and Hibernate support query hints. I use the QueryHints annotation on a repository method or call setHint on a query, for things like timeout, read-only mode, or enabling the query cache. If I need a database-level optimizer hint, I drop to a native query and write the hint as an SQL comment.

4. Monolithic vs Microservices
A monolith packages everything into one deployable app — simple to deploy and fast with in-process calls, but tightly coupled and hard to scale one part alone. Microservices split the system by business capability into independently deployable, independently scalable services, but you pay with distributed complexity: network calls, data consistency, and harder debugging. So I'd start with a monolith for a small team and move to microservices when scale or team size demands independent deployment.

5. Stored procedures or Java/Hibernate logic?
I put business logic in the Java and Hibernate layer by default — it's easier to maintain, unit test, version control, and debug, and it keeps the app database-independent. Stored procedures shine for data-heavy batch work where I want to avoid network round trips. So my rule is normal logic in Java, stored procedures only for heavy, performance-critical data operations.

6. person(name, age) — return the name and age of the oldest person
SELECT name, age
FROM person
ORDER BY age DESC
LIMIT 1;
The gotcha: you can't write SELECT name, MAX(age) without GROUP BY — strict mode errors out, otherwise the name is mismatched. To handle ties for the oldest, use a subquery:

SELECT name, age
FROM person
WHERE age = (SELECT MAX(age) FROM person);
7. order + customer — largest price in the last 10 years, return price + customer name
SELECT c.customer_name, o.price
FROM orders o
JOIN customer c ON o.customer_id = c.customer_id
WHERE o.order_date >= CURRENT_DATE - INTERVAL '10 years'
ORDER BY o.price DESC
LIMIT 1;
Join to get the customer name, filter to the last ten years, order by price descending, take the top row. To return all rows tied for the highest price, replace LIMIT with a price = (SELECT MAX(price) ...) subquery.

8. Eureka annotations & configuration in Spring Boot
On the Eureka server I add the EnableEurekaServer annotation and configure it not to register with itself. On each client I set the application name and point its service-url to the Eureka server, so it auto-registers. Then for service-to-service calls I use a load-balanced RestTemplate or OpenFeign, which resolves other services by their registered name instead of a hard-coded URL.

9. What is your responsibility in the microservices?
I owned one service end to end — for example the student service. That meant designing its REST endpoints, writing the business logic, modeling entities with Hibernate, and handling how it talks to other services through REST or Feign. I also added validation, centralized exception handling, and monitoring with Actuator and Prometheus so the service was observable in production.

10. A → B → C, some return 500/errors — what do you do?
The key is fault tolerance so one failing service doesn't cascade and take down the whole chain. I add timeouts so callers don't hang, retries for idempotent calls, and a circuit breaker with Resilience4j that trips when a downstream keeps failing and returns a fallback instead. I also use distributed tracing with a trace ID so I can quickly see which hop — A, B, or C — is actually returning the 500.

11. How do you secure communication in microservices?
First I encrypt traffic with TLS, and for strong trust use mutual TLS so both sides authenticate. For authorization I pass a JWT or OAuth2 token between services and validate it, often centrally at the gateway. I keep internal services off the public network so only the gateway is exposed, and store secrets in a vault instead of code. At larger scale a service mesh like Istio can handle mutual TLS automatically.

12. When to use a message queue between services?
I use a message queue when services don't need an immediate response and I want to decouple them — async tasks like sending emails or notifications, smoothing out traffic spikes by buffering, and event-driven broadcasts where one event fans out to many consumers. It also gives reliable delivery with retries. I avoid it when the caller needs a strong, immediate, synchronous result; for that a direct REST call is better.


3. mock questions review + recording 
  a. prepare script and included into .md file
  b. recording the practice 
  c. upload to S3
  d. attach S3 link in .md file 
  e. upload .md file to github HW repo. "
